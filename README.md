# 🎬 IMDB-Lite: MERN Auth & Movie App

A full-stack web application built with the **MERN Stack** (MongoDB, Express, React, Node.js). This project features a complete Authentication system including **Login**, **Registration**, and a secure **Password Reset flow**.

## 🚀 Features

* **User Authentication:**
    * Sign Up (Register new users with hashed passwords).
    * Login (Secure authentication with error handling).
* **Password Management:**
    * Forgot Password (Generates a secure reset link).
    * Reset Password (Updates user credentials in the database).
* **Modern UI:**
    * Responsive design using **Tailwind CSS**.
    * Dark mode aesthetic (Movie-themed).
* **Backend:**
    * REST API built with Express & Node.js.
    * MongoDB Atlas for cloud database storage.

## 🛠️ Tech Stack

* **Frontend:** React, Vite, Tailwind CSS, Axios, React Router.
* **Backend:** Node.js, Express.js.
* **Database:** MongoDB Atlas (Cloud).
* **Security:** Bcrypt.js (Password Hashing), CORS.

---

## ⚙️ Installation & Setup

Follow these steps to run the project locally.

### 1. Clone the Repository
```bash
git clone [https://github.com/YOUR_USERNAME/reset-app.git](https://github.com/YOUR_USERNAME/reset-app.git)
cd reset-app